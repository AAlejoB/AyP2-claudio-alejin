with Alumnos;
use alumnos;
package Interfaz is
   -- solo para aliviar el programa principal.
   function interfazOpciones return Character;
   procedure Interfazaltaalumno(A: in out Talumnos);
   procedure Interfazbajaalumno(A: in out Talumnos);
--   ##etc
--   ##   
end interfaz;
